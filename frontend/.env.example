# Apex Sales AI - Frontend Environment
# Leave empty to use the Vite reverse proxy (recommended for local/preview).
# Set an absolute URL only when the API is on a different origin.
VITE_API_BASE_URL=